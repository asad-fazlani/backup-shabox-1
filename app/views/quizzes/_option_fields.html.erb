<div class="form-group">

				<%= f.label :option %><br/>
 				<%= f.cktext_area :opt_name, :ckeditor => {:toolbar => 'pure'}, ckeditor: { language: 'us'},id: "float-right#{rand(5)}" %>

</div>
			<div class="form-group form-check">
				<%= f.check_box :is_answer, class:"form-check-input" %>
				<label class="form-check-label">Set Answer</label>
</div>
<% questionid = questionid.present? ? questionid : nil  %>
			<%= f.hidden_field :question_id, value: questionid %><!--Pass the question id to the controller-->
